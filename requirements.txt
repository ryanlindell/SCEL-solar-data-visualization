requests>=2.31
python-dotenv>=1.0
PyYAML>=6.0
openpyxl>=3.1
pytest>=8.0
websocket-client>=1.6  # only for the optional browser tests (tests/browser)
pandas>=2.0  # sensor_model/ only: the sensor + pvlib estimate, separate from the PVWatts pipeline above
pvlib>=0.10  # sensor_model/ only: pvlib.pvsystem.pvwatts_dc and pvlib.inverter.pvwatts
